# Changelog

> Tool-maintained. Entries below are appended automatically by `/git-clean` when it confirms a ticket's branch has merged and moves that ticket to `done/` — see `CLAUDE.md`'s Project Management System section. Safe to hand-edit (e.g. to group entries under a release heading) but not required; don't remove the marker comment below, `git-clean` looks for it to know where to insert.

<!-- git-clean: insert new entries below this line, newest first -->

*No shipped changes yet.*
